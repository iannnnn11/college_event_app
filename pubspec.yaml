name: college_event_app
description: A college event app made with Flutter
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.4
  cloud_firestore: ^4.15.8
  firebase_storage: ^11.5.0
  image_picker: ^0.8.7+5



dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true


